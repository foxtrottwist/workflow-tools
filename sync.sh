#!/usr/bin/env bash
# Placeholder — MCP server artifacts were removed in v0.21.0.
# Skills are edited directly in the plugin — no syncing needed.
# Run build.sh to validate the full plugin structure.
set -euo pipefail

echo "==> Nothing to sync. Run build.sh to validate."
